<div class="user-info">
    <ul class="">
        {% if userLogin == true %}
            <li>
                <div class="avatar bold-blue-border">
                    <img src="images/avatars/{{ currentUser.avatar }}" width="150" height="150"></a>
                </div>
            </li>
            <li>
                <label for="UserFullName">{{ app.user.username }}</label>
            </li>
            <li>
                <a href="{{ serverUrl }}login">
                    <input type="submit" class="btn btn-default log_out" value="Выйти">
                </a>
            </li>
        {% else %}
            <li>
                <a href="{{ serverUrl }}login">
                    <input type="submit" class="btn btn-default log_in" value="Войти">
                </a>
            </li>
        {% endif %}
    </ul>
</div>

