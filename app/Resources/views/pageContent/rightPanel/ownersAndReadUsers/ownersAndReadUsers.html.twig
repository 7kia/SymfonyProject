<div class="owners-and-read-users bold-blue-border lateral-element blue-text">
    <div class="book-page-title bold-blue-border center-align"><b>Есть у пользователей</b></div>
    <div class="owners-containers bold-blue-border">
        <table class="owner-list" >
            <thead >
            <tr>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% if ownerList|length > 0 %}
                {% for i in 0..(ownerList|length - 1) %}
                    <tr>
                        <td>
                            <div class="owner-container bold-blue-border">
                                <div class="read-user-container__image-container inline-block ">
                                    <img class="image-container_book-image blue-border" src="avatars/{{ ownerList[i].avatar }}"
                                         alt="{{ ownerList[i].name }}"/>
                                    <a href="{{ path('book_page') }}?book_id={{ bookData.id }}&send_application_to={{ ownerList[i].userId }}">
                                        <img class="send_application_image" src="images/sendApplication.png"
                                             alt="Подать заявку"/>
                                    </a>
                                </div>
                                <div class="owner-name-container inline-block blue-border center-align">
                                    <div class="owner-name-container__owner-name">
                                        <a href="{{ serverUrl }}user_book_catalog?book_list_name=personal_books&owner_id={{ ownerList[i].userId }}">
                                            <b>{{ ownerList[i].name }}</b>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
            {% endif %}
            </tbody>
        </table>
    </div>

    <div class="book-page-title bold-blue-border center-align"><b>Читают</b></div>
    <div class="read-users-container bold-blue-border">
        <table class="read-user-list" >
            <thead >
            <tr>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {% if readUserList|length > 0 %}
                {% for i in 0..(readUserList|length - 1) %}
                    <tr>
                        <td>
                            <div class="read-user-container bold-blue-border">
                                <div class="read-user-container__image-container blue-border inline-block ">
                                    <img class="bookImage" src="avatars/{{ readUserList[i].avatar }}"
                                             alt="{{ readUserList[i].name }}"/>
                                </div>
                                <div class="read-user-container__read-user-name-container blue-border inline-block center-align">
                                    <div class="read-user-container__read-user-name">
                                        <a href="{{ serverUrl }}user_book_catalog?book_list_name=personal_books&read_user_name={{ readUserList[i].userId }}">
                                            <b>{{ readUserList[i].name }}</b>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
            {% endif %}
            </tbody>
        </table>
    </div>

</div>
{# links for "top\leftButtons.html.twig"
#}
