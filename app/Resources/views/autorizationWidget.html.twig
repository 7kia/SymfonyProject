{%- block form_errors -%}
    {%- if errors|length > 0 -%}
        {%- for error in errors -%}
            <b>{{ error.message }}</b>
        {%- endfor -%}
    {%- endif -%}
{%- endblock form_errors -%}